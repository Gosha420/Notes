const CACHE='gosha-v31-2026-08-09';
const CORE=['./','./index.html','./v24-patch.js','./v31-fix.js','./manifest.webmanifest','./icon-192.png','./icon-512.png','./icon-maskable-512.png','./gosha-hero.png'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(c=>c.addAll(CORE)).then(()=>self.skipWaiting()))});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
async function withPatch(response){const text=await response.text();let injected=text;if(!injected.includes('v24-patch.js'))injected=injected.replace('</body>','<script src="./v24-patch.js?v=31"></script></body>');if(!injected.includes('v31-fix.js'))injected=injected.replace('</body>','<script src="./v31-fix.js?v=31"></script></body>');const headers=new Headers(response.headers);headers.delete('content-length');headers.delete('content-encoding');return new Response(injected,{status:response.status,statusText:response.statusText,headers})}
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;event.respondWith((async()=>{try{const response=await fetch(event.request);if(event.request.mode==='navigate'){const patched=await withPatch(response);caches.open(CACHE).then(c=>c.put(event.request,patched.clone()));return patched}caches.open(CACHE).then(c=>c.put(event.request,response.clone()));return response}catch(_){const cached=await caches.match(event.request)||await caches.match('./index.html');if(cached&&event.request.mode==='navigate')return withPatch(cached);return cached}})())});